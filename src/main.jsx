import React from "react";
import ReactDOM from "react-dom/client";
import MinutesManager from "./MinutesManager.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MinutesManager />
  </React.StrictMode>
);
